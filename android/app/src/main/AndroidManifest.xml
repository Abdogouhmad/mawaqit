<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Alarm-card vibration (the native alarm notification buzzes even if the
         media volume is off — the Adhan clip itself plays on the alarm stream). -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Adhan must break through the lock screen when the device is asleep:
         the full-screen intent wakes the display and shows the adhan card over
         the lockscreen, and the wake lock keeps playback alive while the
         screen is off. USE_FULL_SCREEN_INTENT is a normal (auto-granted)
         permission on API 29+; SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM below
         gate the exact-alarm scheduling itself. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- "Force the adhan": Do Not Disturb access lets a ringing call lift
         silent mode (and it is restored the moment the adhan stops), while the
         battery-optimisation exemption keeps AlarmManager out of the Doze
         delays that swallow adhans and pre-prayer cards on stricter OEM builds.
         Both are requested explicitly from Settings — see AlarmAccess.kt. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- "Force the adhan" while the phone is IN USE. Android deliberately
         downgrades a full-screen intent to a heads-up card when the screen is
         unlocked, and blocks a background startActivity() from the alarm
         receiver when another app is in the foreground — so on an unlocked
         phone the adhan would otherwise only ever be a tray card. "Display over
         other apps" is the one user-grantable exemption from the
         background-activity-launch restriction, which is what lets
         AdhanScheduler.forceShowAdhan() put the presenter on screen from there.
         Requested on demand as an "Alarm reliability" row; the app is fully
         functional without it. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Native Adhan alarm engine: the foreground service owns the Adhan clip
         so it keeps ringing after the Flutter process is killed. It is declared
         `specialUse` rather than `mediaPlayback` on purpose: a mediaPlayback
         service is treated as music by the platform, which made the adhan show
         up as a media-player card (with playback controls, and in the media
         output switcher) instead of an alarm. The subtype below is the
         justification shown in the Play Console. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Prayer reminder card (exact alarms + progress updates) -->
        <receiver
            android:name=".PrayerReminderReceiver"
            android:enabled="true"
            android:exported="false" />
        <!-- Mute an individual prayer occurrence from the card (FIX 5) -->
        <receiver
            android:name=".MutePrayerReceiver"
            android:enabled="true"
            android:exported="false" />
        <!-- Native Adhan alarm engine: exact-alarm broadcast fires the alarm
             (foreground service + full-screen shell takeover) even when the
             app is killed. -->
        <receiver
            android:name=".AdhanAlarmReceiver"
            android:enabled="true"
            android:exported="false" />
        <service
            android:name=".AdhanPlaybackService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="prayer_adhan_call" />
        </service>
        <!-- Re-arm today's adhans and reminders after a reboot or an app update:
             AlarmManager drops every alarm when the package is replaced, and the
             12-hourly WorkManager task can be up to half a day away from
             re-arming them — which is how a phone comes back from a restart
             with no adhan scheduled at all. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
        <!-- OTA update file provider (package installer handoff) -->
        <provider
            android:name=".OtaUpdateFileProvider"
            android:authorities="${applicationId}.ota_update_provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>
        <receiver
            android:name="com.mawaqit.mawaqit.PrayerWidgetHomeWidgetReceiver"
            android:label="@string/home_widget_prayer_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_home_widget" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>